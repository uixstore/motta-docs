# Included Files

When you purchase our theme on ThemeForest, you need to download the theme package from your themeforest account. Navigate to your downloads tab on ThemeForest and find Motta. Click the download button to see the two options. The **All files & documentation** contain everything, the **Installable WordPress** Theme is just the installable WordPress theme file. Below is a full list of everything that is included when you download the main files, along with a brief description of each item.

- `motta.zip`: main theme file that need to be uploaded to host to install Motta theme.
- `motta-child.zip`: the basic child theme file for people want to customize the theme.
- `Documentation`: documentation folder that contains documentation files.
- `Plugins`: contains all plugins which are required/recommended by this theme.
- `Demo Data`: folder contains demo data files which are exported from the demo site.
	- `soo-demo-importer.zip`: the plugin for "one click import demo".
	- `Data`: folder contains content files for manually import demo content
		- `demo-content.xml`: the XML file for importing demo content
		- `widgets.wie`: the file contains exported widgets
		- `customizer.dat`: the file contains exported settings for the theme
